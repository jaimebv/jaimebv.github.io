Act as an expert frontend developer and elite UI/UX designer. Create a premium, dark-themed, single-page portfolio and CV for an IoT Edge Technical Architect. The design must inspire immense trust, technical responsibility, and elite expertise.

Follow this strict design system called "Modern Obsidian" to the letter:

1. Global Rules & Constraints (CRITICAL)

Color Palette: Strictly monochromatic (#080808 to #FFFFFF). ABSOLUTELY NO standard colors (no blue, red, green, etc.). Use Silver Gradient for accents: linear-gradient(135deg, #F8FAFC 0%, #94A3B8 100%).

Layout Container: Use a fluid width of 92vw for all main containers instead of standard max-width containers to maximize editorial impact. Implement extreme spacing.

Depth & Shadows: DO NOT use standard box-shadows. Use borders (rgba(255, 255, 255, 0.08)) or backdrop-blurs (backdrop-filter: blur(24px)) to create depth.

Background Effect: Apply an SVG fractal noise overlay at 0.05 opacity across the entire page body.

Animations: Use cubic-bezier(0.16, 1, 0.3, 1) with a 0.8s duration for all entry/slide-up animations.

2. Typography System

Headlines (Serif): 'DM Serif Display'. MUST be italicized, tracking-tighter, leading-[0.85].

System/UI (Mono): 'Geist Mono'. MUST be used for all technical labels, metadata, nav links, and buttons to maintain an engineered feel. Sizes 10px-14px. Uppercase tracking: 0.2em to 0.5em.

Body (Sans): 'Inter', font-weight 300-600, sizes 14px-18px for maximum readability.

3. Component Specifications

Header / Navigation:

Full width (92vw), padding: 32px.

Left side: Monospace brand logo with uppercase tracking.

Right side (Desktop): Flex container with border-separated links (bg-white/5, border-white/10) and a solid white 'Contact' button.

Mobile: Minimalist hamburger icon or 'MENU' monospace label.

Hero Section:

Centered layout within the 92vw container. Faint frosted glass background (opacity-30, blur) with a massive 4rem border radius (desktop) and 2rem (mobile).

Content: Massive serif headline (clamp: 42px to 140px) stating the title, with an italicized silver-gradient span for emphasis.

Below headline: Fluid three-column 'Metadata Bar' in monospace.

Padding: py-12 to py-32 depending on viewport.

Experience & Skills (Bento Feature Grid):

Grid layout: 1 col (mobile), 2 col (md), 4 col (lg).

Borders: Create a technical grid look using border-y and border-r with white/10. Cards have a 1rem (16px) radius.

Hover: Background white/0.03, border white/0.2.

Content: Top-left index (e.g., "01 / DISTRIBUTED SYSTEMS") in 10px monospace. Followed by a 4xl serif headline and monospace body text detailing the technical achievement or skill.

Contact / Intake Form (Beta Capture Style):

Max-width: 2xl container, horizontal flex (sm:row).

Wrapper: Frosted-glass, 2xl-rounded container.

Input Field: Transparent background, font-mono text, no border, 100% width.

Submit Button: Silver gradient background, high-contrast black text, font-mono bold, uppercase tracking, rounded-xl, 1px lift on hover with box-shadow: 0 0 20px rgba(255,255,255,0.15).

Floating Mobile Bottom Nav:

Appears only after scrolling past the Hero section.

Position: Fixed, bottom-6, centered.

Style: Pill-shaped, blurred 24px, obsidian background at 85% opacity.

Content: 5-item flex layout. Middle item is a primary action (white bg, black text). Others: Icon + 8px monospace label.

Transition: Start at translate-y-32 and opacity-0, sliding up smoothly.

IMPORTANT: Do not deviate from the typography, layout constraints, or the strict monochrome/silver palette.